# 최종 상태 검토 — 전 브랜치 대조

검토 2026-08-02 · 담당영역 QA·독립검증
기준 `origin/main` `34e4606` · 인계 브랜치 `7caae1d`

---

## 1. 결론

```
검증 재실행       전부 통과 — 최종 상태에서도 값이 바뀌지 않았다
QA 리뷰 반영      v5.2 로 1건 해소 · 1건 미해소
신규 QA 대상      KR9 정기보고서 인계 패키지 (독립 QA NOT_STARTED)
미이행            QA 요청 3건 — 기업행사 조회 창 · 079550 등재 · 룰북 정정
```

---

## 2. 검증 재실행 — 최종 상태 기준

| 검증 | 결과 |
|---|---|
| corrected-run 독립검증 (`verify_ca16_4.py`) | **PASS** |
| 인디케이터 v5.2 교차대조 `2026-03-24` | `1.2734220297227782e-16` · status·seasoning 일치 |
| 인디케이터 v5.2 교차대조 `2026-06-23` | `1.8765659201215791e-16` · status·seasoning 일치 |

**v5.1 과 값이 같다.** `MARKET_CLOSED` 가 실제 데이터에 0건이므로 v5.2 수정이
현 산출물의 값을 바꾸지 않는다. 예상된 결과이며, 바뀌었다면 오히려 문제였다.

### 인계 브랜치가 산출물을 건드리지 않았다

`output_f1` 11개 파일 전량이 `origin/main` 과 해시 동일하다.
`engine/run_pilot.py` 도 차이 없다. **인계 브랜치는 문서만 추가한다.**

---

## 3. QA 리뷰 반영 상태

### 해소 — v5.2 `34e4606`

```python
- listed = g[g["daily_market_state"] != "NOT_LISTED"]
+ # 관측창 = 개장일 행만. NOT_LISTED(미상장)·MARKET_CLOSED(휴장)는 창 구성에서 제외
+ # (엔진 그리드는 휴장 행을 생성하지 않으나, 임의 입력 방어 — 260731 QA 리뷰 반영)
+ listed = g[~g["daily_market_state"].isin(["NOT_LISTED", "MARKET_CLOSED"])]
```

주석이 **왜 지금은 무해한지**와 **왜 그래도 고치는지**를 함께 적었다. 정확한 기록이다.

### 미해소 — 결과 저장이 Windows 에서 죽는다

`crosscheck_vs_engine.py:34` 가 그대로다.

```python
with open(f"{__file__.rsplit('/', 1)[0]}/crosscheck_result.json", "w", ...)
```

Windows 경로 구분자는 `\` 라 `/` 로 나뉘지 않는다. 대조 계산이 끝난 뒤
저장 단계에서 `FileNotFoundError` 로 종료된다. **콘솔 출력은 정상이므로
값에는 영향이 없고, 팀 Windows 환경에서 결과 파일 재생성만 막힌다.**

```python
import os
out_path = os.path.join(os.path.dirname(os.path.abspath(__file__)),
                        "crosscheck_result.json")
```

---

## 4. 신규 QA 대상 — KR9 정기보고서 인계 패키지

브랜치 `인계/KR9-정기보고서-1차완결-20260801` · 145파일

```
제작자 담당범위   완료      FIRST_PARTY_COMPLETE
산출물 상태      1차 완결   READY_FOR_INDEPENDENT_QA
독립 QA         미착수     NOT_STARTED          <- QA 차례
팀 승인         대기       PENDING
엔진 승격        차단       BLOCKED_UNTIL_APPROVAL
```

승인경계 표기가 정확하다. 기술 PASS 와 팀 승인을 분리했고 승격을 막아 두었다.

### 4-a. 당좌비율 값은 아직 없다

패키지 전량에서 `quick_ratio`·`당좌`·`유동부채` 검색 결과 **0건**이다.
`quick_ratio_input_status.csv` 도 여전히 `MISSING_INPUT_LEDGER` 다.

**이 패키지는 원자료·무결성 단계다.** XBRL 을 확보했으므로 당좌비율 산출의
**원천**은 마련됐으나 값 원장은 후속이다. 재무 게이트는 계속 미가동이다.

### 4-b. 회사별 자료 상태 — 이상 2건

| 회사 | PDF | XBRL | 직접대응 | PDF_ONLY | 비고 |
|---|---:|---:|---:|---:|---|
| 효성중공업 298040 | 24 | 24 | **0** | **24** | 과거 PDF_ONLY·소급대응 미수행 |
| LG CNS 064400 | 24 | **11** | 11 | **13** | — |
| 그 외 7사 | 19~24 | 동수 | 전건 | 0~2 | 정상 |

**효성중공업이 구조적으로 다르다.** XBRL 이 24건 있는데 직접대응이 0 이다.
`5D-B4C-5C` 가 사유를 밝힌다 — *"효성중공업 PDF_ONLY 원수집 계보확정 /
현재 XBRL 의 별도 수집계보"*. 즉 **PDF 와 XBRL 의 수집 계보가 다르다.**

여기서 재무값을 뽑으면 **PIT 계보 보증이 없는 자료**가 된다. 당좌비율 원장
구축 시 이 종목만 근거 경로가 달라지므로, 값을 쓰기 전에 계보를 확정해야 한다.

LG CNS 는 2025년 상장이라 과거 정기보고서의 XBRL 부재가 설명될 수 있으나
**확인 대상이다.**

### 4-c. 미해소 단계

```
5D-B4A     PARTIAL_PASS_SCOPE_MISMATCH            대응표 원본 위치 복원 미완
5D-B4C-4   PASS_WITH_ATTENTION                    1개 회사 잔여 XBRL 검토
5D-B2      TECHNICAL_PASS · 잔여 93건             파일명 기반 구조검토
```

---

## 5. 미이행 — QA 가 요청했던 것

| | 요청 | 일자 | 상태 |
|---|---|---|---|
| 1 | 기업행사 조회 창 `2026-06-30` 까지 재실행 | 07-31 | **미실행** |
| 2 | `079550` 유상증자 원장 등재 | 07-31 | **미실행** |
| 3 | 룰북 218행 "유일 행렬" 표기 정정 | 07-30 | 미등재 |
| 4 | 룰북 197행 백분위 보간 방법 명문화 | 07-30 | 미등재 |
| 5 | 수집 창 ≥ 산출 창 규칙 명문화 | 07-31 | 미등재 |
| 6 | ratio / factor 표기 규약 확정 | 07-31 | 미등재 |

결정로그 최신은 `D-20260731-01`·`D-20260731-02`(BM 출처)뿐이다.

**1·2 가 열려 있으므로 기업행사 원장은 `receiver_confirmation = PENDING` 유지다.**
다만 `079550` 은 원문 판독으로 **파일럿 무영향이 확정**됐으므로
(제3자배정·납입일 `2026-09-16`·자료마감일 이후 공시) **지수 재산출은 불필요하다.**

`4`(백분위 보간)는 `engine/config.py:13` 에 `PERCENTILE_METHOD = "linear"` 로
고정되고 U-2 를 인용했으나, 주석이 *"현행 엔진·QA 와 일치"* 라
데이터사전 v0.8 §11 이 금지한 논리 형태다. **조문화는 여전히 필요하다.**

---

## 6. 파일럿 최종값 — 변동 없음

```
지수        1337.7491   (+33.77%)
BM          1417.7850   (+41.78%)
BM 대비     −8.0036%p
분할 효과   +6.6611%p
독립 대조   1.056e-15   (RTOL 1e-12)
```

인용 조건도 그대로다 — as-run 병기, 보정본 단독 인용 금지,
`PERFORMANCE_NOT_FROZEN`.

### 발표에서 쓰면 안 되는 표현

```
"테마·유동성·재무 3축 게이트로 선별했다"
   -> 파일럿 발동은 시즈닝·유동성 2종. 재무는 원장 부재로 미가동
"리밸런싱 로직을 검증했다"
   -> 적용 이벤트 0회. 기전은 검증됐으나 실적용 실증은 미수행
"알파가 유의하다"
   -> t = −0.372
```
