# 권보성님 엔진 인계안내

이 폴더는 회사별 자료가용성·대응상태·승인경계를 엔진 입력계약과 연결하기 위한 인계본이다.

## 원칙

- 패키지 자체는 1차 완결본이다.
- `검토후보값`, `팀승인값`, `엔진입력값`은 서로 다른 필드로 유지한다.
- PDF_ONLY와 XBRL_ONLY를 결측 0으로 해석하지 않는다.
- 승인 전 후보값은 production 입력으로 승격하지 않는다.
