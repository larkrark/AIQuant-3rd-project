# 세션 인수인계 (2026-07-23 저녁 기준)

다른 Claude 세션에서 작업을 이어받기 위한 문서. **먼저 읽을 것**: 결정로그(단일 원본, D-01~D-13) · 데이터사전 v0.2.1 · pilot-status.html(현황판) · 260723_1차중간점검_대비_실행계획.md · 06_코드/engine/README.md · input_data/INPUT_MANIFEST.md — 전부 repo(위키)에 있음.

## 프로젝트 한 줄

한·미 우주·AI·에너지 테마형 지수 — 규칙 기반 패시브 커스텀 인덱스 (팀 4인: 권보성 팀장·김근형·추주원·김민호). 내일 **7/24(금) 오후 1차 중간 점검** — 필수 제출물: 엔진 구현 + pitch-deck.

## 절대 원칙 (위반 금지)

1. 결정 이력의 단일 원본 = 결정로그, 산출물·코드 원본 = repo. 기록에 없는 연결·추론을 사실처럼 쓰지 않는다(추론은 "제 제안/제 추론"으로 표시).
2. 임의 숫자 금지 — 모든 잠정값은 D-13 동결표(rule_version=v0.9-pilot)에 기록된 것만 사용. **성과를 본 뒤 규칙 조정 금지** — 발견 문제는 결정로그 미결 등록만.
3. 합성 표본(sample_data) 수치는 어떤 문서에도 인용 금지. 파일럿 결과는 간이투자설명서의 [백테스트 후 채움] 칸에 넣지 않음(그 칸은 정식 백테스트 몫).
4. `.env`(KRX·ECOS 자격증명) 커밋·채팅 공유 금지 (repo는 public).
5. 공유 산출물에 개인 실명 기록 금지(익명 슬롯·역할코드).

## 현재 상태 (7/23 18시 무렵)

- **결정**: D-13 일괄 동결 완료(잠정값 8항목·제59조 복원·달력 공통거래일 역산·입력계약·결과표→의결 매핑 5건·수집 담당 v1.2). pitch-deck 골격·주제 확정(「한·미 우주·AI·에너지 테마형 지수 — 커스텀 인덱스 엔진 기반 투자 상품화」).
- **엔진**: `06_코드/engine` v0 골격 9파일, 합성 데이터 관통 시험 통과. 입력계약 8파일 스키마 = run_pilot.py 상단 주석.
- **수집**: 보성분 완료 — prices_us(9종목)·listings_us_fixed(드라이런 사용본, draft는 폐기)·calendar(KR 180/US 187 개장일)·bm_kr(KRX 공식 180행)·bm_us(^RUA 187행)·fx(ECOS 181행). KRX300 예비만 보류(비필수). 출처는 INPUT_MANIFEST.md.
- **한국분 도착**: 근형님 KR9 인계 5파일 → `input_data\kr9_handoff\`에 원본 배치(사용자 수행 예정/완료 확인 필요). 매니페스트 경고: entity_id 9건 HOLD, 상장일 전부 PROXY(WARN·PILOT_ONLY), 일부 종목 수정주가 행 한도, ADTV90 0반영은 TEAM_CONFIRMATION_PENDING 표기(단 D-13 ①에서 파일럿 잠정 승인됨).
- **미완**: 민호님 독립 스캘레톤 작성 중(별도 브랜치, 코드 비공유 원칙 — 엔진 코드를 보여주지 말 것). 주원님은 내일 09:00 분포표로 복귀.

## 내일(7/24) 작업 순서 — 시간표는 실행계획 문서 기준

1. **병합**: kr9_handoff의 prices(한국, KRX 거래대금 포함) + prices_us → `prices.csv` / seed_basket KR9+US9 합본(18종목) / listings 병합(listings_us_fixed 사용) / halts는 근형분 그대로. 주의: 근형 halts.csv의 full_day_halt가 `True` 문자열 — 엔진은 1/0·불리언 모두 fillna 후 int 변환하므로 `True`→1 변환 확인 필요.
2. **드라이런 → 분포표** (09:00): 상태코드→시즈닝→ADTV90 → 시장별 분포 → **P10 잠정 하한 고정·기록** (주원 담당·민호 검토).
3. **본 실행** (09:30~11:00 최소 완주선): `python run_pilot.py input_data output` → 구성내역·가중·지수+BM. 산출물 패키징·위키 커밋(12:00까지).
4. **pitch-deck**: 5면(엔진 캡처)·7면(지수 vs BM 라인차트, 1000 리베이스) 채움 → pptx 변환(간이투자설명서 슬라이드 v0.1 디자인 재사용 — 네이비 1E2761·6셀 모티프).
5. **QA**(민호, 09:30~): input_data 스냅샷 보존 → 수기 검산(1종목×5일+리밸 1회) → 독립 재산출 비교.
6. 점검 시연 순서: 전략 방향성(문서) → 엔진 v0 데모 → pitch-deck. 점검 후 결과 검토 회의(규칙 조정 논의 금지, 사전 지정된 다음 의결 일정만).

## 점검 후 대기열 (사전 지정된 의결 — D-13 ⑤)

ADTV90 분포표→#16 하한 절대금액 / 셀 잔존수→#19·#21 / 거래대금 오차비율 분포→검산 플래그 임계 / 구성내역 코스닥 비중→BM 재확인 / 가중 결과→안건 H.

## 미세 잔여 항목

통합 룰북 제59조 문안 수정(문서 작성자=근형 반영 대기) · 상품명 가칭·팀명 표기 방식(팀장 결정, pptx 전) · entity_id 매핑·공식 상장일(근형 HOLD 해제 대기) · DRAFT 표시 지위 구분 · 하한후보 L 백분위 위치 열 O/X · SPCX 상장일 EDGAR 확인 · fx 계열코드 확정.

## 세션 운영 방식 (이 프로젝트에서의 Claude 역할)

디스코드 회의 지원(팀원 의견을 확정 기록과 대조 검토, 출처 명시), 결정로그·데이터사전·현황판 유지, 엔진 코드·문서 제작, 커밋은 기기 연결 폴더(`C:\Users\kwon\Documents\AIQuant 3차 프로젝트\...`)로 수행 후 사용자가 git push. 스테이징 캐시가 낡을 수 있으니 파일 검증은 기기에서 직접 확인.
