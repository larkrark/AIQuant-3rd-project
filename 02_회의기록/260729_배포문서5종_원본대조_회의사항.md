# 260729 배포문서 5종 원본 대조 — 파일별 분석과 회의 사항

| 구분 | 내용 |
|---|---|
| 작성일 | 2026-07-29 |
| 대상 | `check_this/20260729/` 5개 파일 (2026-07-28 김근형 배포) |
| 대조 기준 | 저장소 내 직전 버전 + `01_운영문서/결정로그.md`(팀 결정 단일 원본) + `02_회의기록/260728_회의내용_정리_회의안대조.md`(당일 채팅 기록) |
| 문서 성격 | 검토자(독립 QA) 대조 결과. **팀 판단을 대신하지 않으며, "회의 사항"은 의결 대상 후보 제시일 뿐 결정이 아니다.** |
| 검증 수준 | §7에 실측 확인 항목과 미확인 항목을 구분해 기재 |

---

## 0. 원본 매핑

| # | 배포 파일 | 저장소 원본 | 상태 |
|---|---|---|---|
| 1 | `260728_한미3테마_통합룰북_v1.5_역할진행도_발표운영반영_검토본.docx` (323KB) | `01_운영문서/260724_커스텀인덱스_구성및산출_룰북_v0.4_결정반영본.md` (41KB) | **v1.3·v1.4 부재** — v0.4 → v1.5 직접 대조 |
| 2 | `260728_한미3테마_데이터사전_v0.6_정합성표현_상태축수정본.docx` (77KB) | `01_운영문서/데이터사전.md` (67KB, v0.2) | **v0.3·v0.4·v0.5 부재** — v0.2 → v0.6 직접 대조 |
| 3 | `260728_수식정리부록_v1.4_정합성표현_Seed18범위수정본.docx` (773KB) | **없음** | 저장소에 수식부록 원본 0건(`find -iname '*수식*'` 결과 배포본 1건뿐). 룰북 근거자료가 인용한 `260722_수식정리부록_ADTV90_재무_PIT_구성시험.docx`도 부재 → 버전 대조 불가, **타 문서 정합성 대조로 대체** |
| 4 | `260728_프로젝트_회의록_Seed18_진행도_발표운영_결정반영본.docx` (217KB) | 직전 회의록 없음(신규). 대조축 = `02_회의기록/260728_회의내용_정리_회의안대조.md`, `결정로그.md`, `01_운영문서/260722_작업담당표.md` | 회의 사실 검증 방식으로 대조 |
| 5 | `TEAM_SHARE_MD_REVIEW_SUMMARY.md` (2.4KB) | **없음** — 대상 코퍼스(md 99개, `copied_markdown/`)가 저장소에 없음 | 요약본 자체만 검토 |

**중간 버전 3개(룰북)·3개(데이터사전)와 수식부록 원본이 저장소에 없다.** 결정로그 운영원칙 3항("검토한 대안과 기각사유를 함께 기록한다 — 변경관리 기록")과 룰북 J항 동기화 순서를 적용하려면 중간본 또는 변경이력이 필요하다.

---

## 1. 통합룰북 v1.5-review

### 1-1. 구조 변화

| 항목 | v0.4 | v1.5 |
|---|---|---|
| 분량·형식 | 394행, 0~19장 서술체 | 906행, 제1~10편 제1~143조 조문체 + 별표 1~14 + 부칙 + **개정장 2개(A~D, E~J)** |
| 결정로그 연결 | §16 ID 대응표(M-01~M-08→D-번호) + **§19 변경 대조표 24건 전부 D-번호 명시** | **D-번호 인용 1건**(`D-20260720-08`). 변경 대조표·ID 대응표 삭제, `U-01~U-06 채팅근거표`가 대체 |
| 상위문서 | 동반 문서에 `Mandate.md` 명시(3회) | **`Mandate` 0회** |
| 파일럿 값 취급 | 머리말 `룰북에는 규칙 구조만 반영하고 파일럿 숫자는 포함하지 않는다` | 개정장 A·B·G에 실측값 직접 수록(자료마감일 2쌍, KTOS −0.55%, 15/3 편입결과, N=1 셀 1/6) |
| 미결 관리 | §17 안건 A~K 단일 목록 | 별표10 T-01~T-08 / 제141조의3 H-01~H-06 / 제141조의4 회의확인 10건 / I절 8건 — **4개 목록 분산, 전부 담당·기한 없음** |

### 1-2. 확정 결정이 되돌려진 곳 (7건)

| # | 사안 | v0.4 / 결정로그 | v1.5 |
|---|---|---|---|
| 1 | **BM 지역비중** | D-20260721-10 ④ `[확정, 전원 동의]` — "공식 합성 BM의 지역비중은 지수의 정기변경 목표 지역비중과 동일 연동(재량 승인이 아닌 규칙 산출값)". **기각 사유란: "BM 고정안: 예외 효과가 알파로 오독됨"** | 제124조 "두 후보를 함께 산출한다… `fixed_5050_benchmark`… `synced_weight_benchmark`… 팀 의결 전까지 APPROVAL_PENDING" — **기각된 안이 대등 후보로 부활** |
| 2 | **ADTV90 하한 분포 모집단** | v0.4 §8.1(199행) "분포 모집단은 시장별 **전체 유니버스**(보통주·시즈닝 통과)" | 제67조(303행) "주 분포는 **테마 통과 후보** 중 A-1을 충족하고 ADTV90이 정상산출된 집합으로 한다" `[확정]` — 모집단이 바뀌면 P5~P75 값 전체가 달라짐 |
| 3 | **시장범위·제외증권** | D-12 ① `확정`(코스피+코스닥), D-11 #3 `확정`(KONEX·우선주·ADR·리츠 제외) | 제13·15·16조 `[PILOT_CONFIRMED / DECISION_LOG_ID_PENDING]` — 결정로그 ID가 이미 있는데 "대기"로 표기 |
| 4 | **셀 부족 재배분** | D-10 ③ `확정` | 제109조 `[PILOT_STRUCTURE / DECISION_ID_PENDING]` |
| 5 | **대표증권 근거승계** | D-12 ② `확정`(발행회사 기준 승계) | 제21조 `[구조 보완 / APPROVAL_PENDING]` + 신규 조건 추가 |
| 6 | **주별 관측일** | D-11 #30 `확정`(각 시장 마지막 개장일) | 제114조 "**후보로 하며**… 2개 분기 달력시험 후 고정" |
| 7 | **공시시각 불명 시 처리** | D-11 #29 `확정` — "data_available_date = 공시일 다음 거래일" | 제28조 "**승인된 시장별 보수적 규칙**에 따라" `[원천별 시간대표 승인 대기]` — 현재 적용할 규칙이 공백 |

### 1-3. 미결이 확정으로 올라간 곳 (2건)

| # | 사안 | 결정로그 | v1.5 |
|---|---|---|---|
| 1 | **환율 세부 4건** | D-08 ③·D-09 ① `[세부 미결 유지]`, 미결 대기열 I·J에 존치 | 제129조(미고시일)·제130조(소급금지)·제131조(수집실패)·제132조(검산계열) 전부 **`[확정]`** ← 실측 확인 |
| 2 | **5개 인디케이터 범위** | v0.4 170행 "최종 5개 인디케이터 인정 범위는 A-4·A-5의 **강사 확인 대기**", 342행 안건 A | 제53조(246행) "v1 인디케이터는 A-1~A-5로 관리한다" `[확정]`. **v1.5에 `강사` 0회** — 미결 항목 자체가 소멸 |

### 1-4. 삭제된 확정·기각 조항 (주요 4건)

| # | 삭제된 것 | v0.4 위치 | 결정 근거 |
|---|---|---|---|
| 1 | **Mandate.md 전체** — "본 문서와 룰북이 충돌할 경우 본 문서가 우선한다" | 동반 문서표 | Mandate 11장 거버넌스. v1.5 제9조 우선순위 체인에 부재 |
| 2 | "ARKK·XLU 등 테마 ETF는 공식 BM이 아니다" | §12.4(277행) | **D-08 ④ 확정** |
| 3 | "PR에 평균 배당수익률을 더하는 추정방식은 사용하지 않음" | §12.2(265행) | **D-08 ⑤ 기각** + Mandate 9조 "PR 지수와 TR BM의 혼용 비교 금지" |
| 4 | AI 보조도구 5항목("AI 산출문을 공식근거로 직접 인용하지 않는다" 등) | §15 | D-03 |

`ARKK`·`근사 TR` 문자열 v1.5 0회 ← 실측 확인.

### 1-5. 내부 모순 (주요)

| # | 위치 A | 위치 B | 충돌 |
|---|---|---|---|
| 1 | 제13·15·16조 `DECISION_LOG_ID_PENDING` | 별표13 "#1 NYSE American 포함 \| **CONFIRMED**" | 같은 규칙이 본문 대기 / 별표 확정 |
| 2 | 제124조 "APPROVAL_PENDING" | 별표9 "정상 지역비중 \| 50:50 \| **확정**" | BM 지역비중 확정 여부 상반 |
| 3 | 제60조 관측창 종료 = `평가기준일` | 개정장 A-3 "`data_cutoff_date`로 고정" | 기준 필드 상이. `평가기준일`은 제27조 날짜필드 정의에 없음 |
| 4 | 제61조 "공식 정책 승인 전 `official_adtv90`은 **생성하지 않고**" | 개정장 E "2단계 … **ADTV90·P10 완료**" | 산출 차단 조항과 완료 보고 병존 |
| 5 | 제141조의2 "최종 편입판정에는 사용하지 않는다" | 개정장 A-5 "18종목 중 **15종목 편입**… 완료로 기록한다" | 편입판정 사용금지 문서에 편입 결과 수록 |

---

## 2. 데이터사전 v0.6-review

### 2-1. 규모

필드 수 v0.2 322개 → v0.6 470개. **신설 156 / 삭제 8 / 공통 314.** 신설 156개 중 결정로그 근거가 있는 것은 소수이고, 대부분은 회의록·채팅 기반이다.

### 2-2. 삭제된 필드 8개 — 전부 결정로그 산물, 별칭 매핑 없음

| 필드 | 근거 결정 | 삭제 영향 |
|---|---|---|
| `adj_close` | v0.2.2 등재(D-07) | §4.1.1 가격 열 사용 목적표 6행 중 4행이 이 필드를 참조. 목적표째 삭제, `provider_adj_close`·`return_price`·`price_adjustment_scope` 3개로 분산되나 **대체 매핑표 없음** |
| `excluded_conditional_accounts` | D-12 ⑥ | 없으면 `CALCULATED_LOWER_BOUND` 값의 재현·검산 불가 |
| `final_approval_status` | D-13 ⑥에 명시 등장 | `team_approval_status`로 대체 추정이나 별칭표에 행 없음 |
| `pub_lag` | D-12 ⑩ | 별칭표 행 삭제 → 이전 산출물 매핑 상실 |
| `trading_value_discrepancy_ratio` | **D-13 ② 확정 필드** | `trading_value_difference_rate`로 개명, 별칭 없음. 추가로 "KRX값>0인 날만 산출, 그 외 NA" 조건이 신정의에서 삭제 |
| `voting_right_source` | D-12 ② "선택 기록 8항목" 중 1개 | 8항목 중 1개 결손 |
| `security_replacement_reason` | D-12 ② "교체 기록 6항목" 중 1개 | 6항목 중 1개 결손 |
| `approval_status`(정식 행) | D-13 ⑥·룰북 v0.4 300행 참조 | §0.4 상태축 표에만 잔존 + 허용값 전면 교체 |

**필드 외 삭제 블록**(전부 결정로그 확정문): TRADING_HALT 우선 적용 3문장(D-12 ⑤) / 유동 금융자산 S 4단 분류표(D-12 ⑥) / 보수적 하한값 6개 산출조건 + "하한값 미달만으로 자동 제외 금지"(D-12 ⑥) / 타이브레이커 고정 서열 리터럴 AI_ROBOTICS=1·ENERGY_POWER=2·SPACE_DEFENSE=3(D-12 ⑧ "리터럴 동결") / 부록 A 인계 3파일 스키마(D-13 ④) / 부록 B 용어(D-13 ⑦) / 부록 C 달력 축(D-13 ⑧) / §22 변경 이력.

### 2-3. enum 다중정의 11건 — 엔진 입력계약 검증 불가

같은 필드가 문서 내 두세 곳에서 다른 허용값을 갖는다.

| 필드 | 세트 1 | 세트 2 | 세트 3 |
|---|---|---|---|
| `seasoning_status` | `SEASONED·SEASONING_INCOMPLETE` | `PASS/FAIL/HOLD` | — |
| `adtv90_status` | 4값(CALCULATED·SEASONING_INCOMPLETE·CALCULATION_HOLD·NOT_APPLICABLE) | 3값(CALCULATED/HOLD/NA) | — |
| `quick_ratio_status` | `CALCULATED_LOWER_BOUND`·`NOT_APPLICABLE` | `LOWER_BOUND`·`NA` | — |
| `selection_status` | `SELECTED/NOT_SELECTED/HOLD` | `INCLUDED/EXCLUDED/HOLD` | `+NOT_JUDGED` |
| `liquidity_gate_status` | `…NOT_APPLICABLE` | `…NOT_APPLIED` | — |
| `benchmark_source_status` | `OFFICIAL·PROVISIONAL·SUBSTITUTE·MISSING` | `SOURCE_FREEZE_PENDING/OFFICIAL` | — |
| `performance_status` | `AS_RUN_REPRODUCIBLE/PERFORMANCE_NOT_FROZEN/FINAL` | `PERFORMANCE_NOT_FROZEN/FROZEN` | — |
| `valuation_price_source` | 조건부·4값 | 필수 Y·2값+"등" | — |
| `threshold_method` | `SEED18_MARKET_CYCLE_P10` | `MARKET_CYCLE_P10` | — |
| `theme_review_status` | 필수 Y·3값(+HOLD) | 2값 | — |
| `weighting_status` | `TEMPORARY·FINAL` | `+HOLD` | v0.2에서 이월된 모순, 미수정 |

추가로 **허용값 열거가 "등"으로 끝나면서 필수 Y인 필드 6종**(`source_sheetization_status`, `financial_diagnostic_status`, `fx_application_method`, `calendar_alignment_method`, `effective_date_method`, `security_type`)과 **허용값 목록 자체가 없는 필수 Y 필드**(`financial_gate_status`)가 있다.

### 2-4. `theme_eligibility_status` — 같은 문서 안 4중 규정

| 위치 | 규정 |
|---|---|
| §8.1 | 독립 enum, 필수 Y, `ELIGIBLE·CONDITIONAL·INELIGIBLE·REVIEW_REQUIRED` |
| 본문 | "이전 필드명 `theme_eligibility_status`는 **읽기 호환용 별칭으로만 유지**하고 신규 산출물에는 `gate_status`를 사용한다" |
| §19 별칭표 | "**독립 필드로 유지**; gate_status 직접 치환 금지" |
| §16.1 | "테마 범위 적격성; gate_status와 독립" |

낮 채팅 §4-1에서 "데이터사전 429행 확인 결과 `gate_status`로 대체된 읽기 호환용 별칭 — D-2의 '독립 축 유지' 질문은 성립하지 않아 회의안에서 해당 문구·질문 삭제 예정"으로 이미 닫힌 사안이다. v0.6이 이를 결정로그 등재 없이 독립 필수 필드로 되돌렸다. 판정원장 스키마가 이 필드에 의존하므로 엔진 구현이 갈린다.

### 2-5. 그 밖의 확인 항목

| 항목 | 결과 |
|---|---|
| **`listing_board`** | **v0.6에 0건.** `board` 0건, "KOSDAQ 비중" 0건. `KOSDAQ`은 `exchange` enum 값으로 1회. → D-13 ③(KOSDAQ 비중 유의 시 KRX 300 재확인)의 발동조건 측정 수단이 여전히 없음 ← 실측 확인 |
| **`approval_status` 기본값 모순** | 허용값 `NOT_REQUESTED/APPROVAL_PENDING/APPROVED/REJECTED`인데 본문은 "기본값은 `approval_status=PENDING` 또는 APPROVAL_PENDING" — **`PENDING`이 자기 enum에 없음** |
| **승인 상태 3계열 불일치** | "범위별 필드는 같은 값 체계를 사용"이라 선언했으나 `record_/team_approval_status`는 `PENDING·APPROVED·HOLD·REJECTED`, `approval_status`·`methodology_approval_status`는 `NOT_REQUESTED/APPROVAL_PENDING/APPROVED/REJECTED` |
| **`role_grade` 5값 전면 개명** | `DIRECT→DIRECT_BUSINESS`, `LIMITED→LIMITED_RELEVANCE`, **`THEME_UNRELATED→THEME_EXCLUDED`**. `THEME_UNRELATED`는 D-11 #8 대비 정합성 확보를 위해 v0.2 병합 수정 ②로 **명시적으로 복원한 값**인데 별칭 매핑 없이 재삭제. §8.1 설명은 여전히 구값 `DIRECT` 사용 |
| **`theme_tiebreaker_stage`** | v0.2가 "`5-1`/`5-2` 표기는 별칭으로만 허용"으로 정합화한 것을 정식값으로 되돌림 + D-12 ⑧ 고정 서열 리터럴 소실 → ⑤-2 적용 시 재현 불가 |
| **boolean 저장 표준** | v0.2 "공식 저장값 `1/0`(D-12 ⑩ 기록 표준). TRUE/FALSE 전환 제안은 **재확정 대기**" → v0.6 "공식 저장값 TRUE/FALSE". boolean 약 25개 필드와 엔진 입력계약 파싱에 영향 |
| **`current_ratio` 신설** | 유동비율은 결정로그·룰북·체크리스트 어디에도 인디케이터·진단지표로 등재된 적 없음. `financial_metric_name` enum(`quick_ratio / current_ratio / NOT_APPLIED`)에 포함돼 사실상 대체 가능 필드로 등록 |
| **하드코딩** | `benchmark_methodology`(필수 Y) = "KOSPI 200 PR + Russell 3000 PR, 정상 50:50" / `split_ratio` = "**010120 확인값 5.0**" / `threshold_method` enum 값 `SEED18_MARKET_CYCLE_P10` / §22 자료마감일 2쌍 `CONFIRMED` |
| **개인정보 원칙 충돌** | "공유용 산출물에는 개인 이름·이메일·계정명을 기록하지 않는다"(유지) ↔ `role_owner`·`presentation_owner`·`slide_owner`·`qna_owner`·`support_owner`·`independent_reviewer` 신설. D-13 ④ "reviewer 실명 대신 익명 슬롯·역할코드"와 충돌 |
| **장번호 중복 7건** | 12·16·17·18·19·20·21이 각각 2~3회 사용 → "데이터사전 §17" 형태의 조항 인용 불가. 결정로그가 "데이터사전 N장 등록"으로 참조하는 방식이 성립하지 않음 |
| **`source_url` 3필드 병존** | `source_url`(§17) / `source_url_or_path`(§8.5) / `source_path`(§2.2). **D-13 ④는 `source_url_or_path` 단일화 확정** |

---

## 3. 수식정리부록 v1.4-review

**저장소 원본이 없어 버전 대조 불가.** 룰북 v1.5·데이터사전 v0.6·결정로그와의 정합성 대조로 대체했다.

### 3-1. 문서 자체 결함

| 항목 | 내용 |
|---|---|
| **식 본체 부재** | (식 1)~(식 62)가 **번호만 남고 본문에 수식 본체가 없다**(고정 이미지 조판). 텍스트로 확인 가능한 식은 개정장 코드블록뿐. 룰북 제136조 "문서만으로 같은 결과가 재현되는지 확인" 요건을 연산명세 문서가 충족하지 못함 |
| **장번호 중복** | 11·12·13·14장이 각각 두 번 등장 |
| **rule_version 미기재** | `rule_version`·`formula_version`이 필드명으로만 등장하고 **실제 값이 한 번도 없음**. 룰북 별표14는 "세 문서를 동일 rule_version으로 연결"을 요구 |
| **상태 어휘 2체계** | 본문 = 확정/적용시험/안건 H 이관/승인대기 4종. 개정장 = `PILOT_CONFIRMED_WITHIN_SEED18`/`PILOT_IMPLEMENTATION`/`APPROVAL_PENDING`/`NOT_PERFORMED` 4종 |

### 3-2. 정합한 수식 (근거 확인됨)

DTV 선택식(D-13 ②) / 차이율 KRX=0이면 NA(D-13 ②) / 당좌비율 QR=(C+S+R)/CL(D-12 ⑥) / 보수적 하한(D-12 ⑥) / TTM 순이익 4분기 합(D-12 ⑥) / K_A·K_B·c_w 8시나리오(D-12 ⑦) / 상한 10%=예시값(D-12 ⑦) / 셀 1/6(D-10 ②) / 잔여비중 전달순서(D-11 #21, D-10 ③) / 대표테마 ①~⑤(D-12 ⑧) / `group_overlap_flag`(D-12 ⑨) / `available_by_cutoff`(D-12 ⑩) / 기준값 1000(D-09 ④) / 90개장일 관측창(D-07). **숫자 예시 (120+30+150)/200=1.50과 `n×c_w ≥ 1/6`, `cw=0.10·n=1 → 잔여 6.67%p`는 산술 검증 통과.**

### 3-3. 근거 없는 수식·상수 (10건)

| # | 항목 | 결정로그 검색 결과 |
|---|---|---|
| 1 | `Seed18`, `U_pilot(m,c)`, `universe_scope=SEED18_LIMITED_PILOT` (14회) | **결정로그에 `Seed18` 0건** ← 실측 확인 |
| 2 | `L_pilot = Q_0.10(ADTV90 : i ∈ U_pilot)` — Seed18 표본 내부 상대 P10 | D-13 ①은 "하한 = 시장별 분포 P10 **잠정**", 모집단은 D-07 "시장별 **전체 유니버스**". 잠정값의 모집단이 축소됨 |
| 3 | `KTOS 약 −0.55%` 경계 진단자료 | 채팅 §7-1에서 "KTOS는 경계사례가 아니라 **분포 최솟값**"으로 **표현 철회된** 사례가 존치 |
| 4 | `split_ratio = 5000/1000 = 5` | 채팅 §7-1 "공시 원문 접수번호는 **확보 중** — 현 근거는 언론 3건", §8-3 "원공시 3.33:1 vs 정정본 5:1". 수식부록은 "확인된 5:1"로 단정 |
| 5 | MACD `12,26,9`, `alpha_n=2/(n+1)` | 결정로그 근거 없음. 룰북 제83조는 `[확정]` 표기 |
| 6 | `I_t = I_(e−) × Σ w·P(t)/P(e)` (CHAIN_REBASE_PILOT) | 근거 없음. 채팅 §4-3 "1.1e-15는 자기재현이지 동치증명 아님" |
| 7 | 평가일 당일 ECOS 환율 | D-08 ③은 원칙만. 채팅 §7-1 "SAME_DAY_ECOS 명세 미흡(어느 시장 기준 '당일'인지, 매매기준율/종가환율 명시 필요)" 미반영 |
| 8 | `BM_fixed` 공식지위 미결 병기 | **D-10 ④에서 기각된 안**(룰북과 동일 사안) |
| 9 | `min_periods=1`, `COMPLETE_90` | 파일럿 구현값 |
| 10 | `minimum_valid_days` | 미정 명시 — 정합 |

### 3-4. 필드명 불일치 — 수식이 존재하지 않는 필드를 참조

| 수식부록 | 데이터사전 실제 | 비고 |
|---|---|---|
| `trade_date`, `trade_status`, `close_price`, `missing_flag` | `market_date`, `daily_market_state`, `raw_close` | `close_price`는 신규 사용 금지 별칭, `trade_date`·`trade_status`·`missing_flag`는 별칭표에도 없음 |
| `cash_equivalents`, `liquid_financial_assets`, `net_receivables_current`, `scope_type`, `missing_code`, `industry_group`, `name` | `cash_and_equivalents`, `confirmed/conditional_current_financial_assets`, `net_current_receivables`, `financial_statement_scope`, `missing_reason_code`, `industry_code/name`, `company_name` | 7건 |
| `legal_entity_id`, `group_source`, `source_reference_date`, `filing_receipt_no`, `group_review_status` | `entity_id`, `group_source_type`, `source_effective_date`, `source_document_id`, `group_status` | `filing_receipt_no`는 데이터사전에 없음 |
| `adtv90_zero_included`, `adtv90_halt_excluded`, `adtv90_diff`, `gate_flip_flag` | `adtv90_zero`, `adtv90_exclude_halt`, `adtv90_difference`, `decision_flip_flag` (룰북 별표4는 또 `adtv90_halt0`, `adtv90_exhalt`) | 같은 값에 **문서 3종이 각기 다른 이름** |
| `tie_breaker_step: PREVIOUS_THEME / CODE_ORDER` | `theme_tiebreaker_stage: 5-1 / 5-2 / NOT_TRIGGERED` | 값 체계 자체가 다름 |
| `CALCULATED_PROVIDED`, `CALCULATED_RECONSTRUCTED`, `PROVIDED_AGGREGATE` | 6.3 허용값에 없음 / `PROVIDER_AGGREGATE` | enum 미등록 |
| `level_gap` (수식부록이 "alpha를 level_gap으로 교정하라"고 지시) | **데이터사전에 `level_gap` 없음** | 지시된 교정 필드가 미등록 |

### 3-5. 산식 불일치

| 항목 | 수식부록 | 대조 |
|---|---|---|
| **차감식 당좌자산** | `QuickAssets = CurrentAssets − Inventories` (재고만 차감) | 데이터사전 6.1 차감식은 `inventory`·`prepaid_expenses`·`other_non_quick_current_assets` **3계정 차감** |
| **효력발생일** | `effective_date = min{ d > selection_date \| KR_open=1 and US_open=1 }` | D-12 ⑩ "선정일 이후 처음 도래하는 **다음 공통 거래주**(사전 지정·룰버전 기록한 **정기변경 적용주**) 중 첫 공통 거래일" — 적용주 조건 탈락 |
| **경계구간** | §4.9가 인접 백분위 사이로 정의, 상태 표기 없음 | 채팅 §7-1: 경계구간 [P5,P10] **전 회차·전 시장 관측치 0건, `NOT_APPLICABLE` 기록방식 채택** — 미반영. 룰북 제78조는 `[DIAGNOSTIC_ONLY / DECISION_ID_PENDING]` |
| **독립성 등급** | §13 `INDEPENDENTLY_MATCHED`, "4~6단계 독립 일치만 완료로 인정" | 채팅 §7-1 "독립 재산출은 **SPEC_ASSISTED**", "자기재현 ≠ 공식 자료원 검증". `independence_level`·`SPEC_ASSISTED`·`QA_REFERENCE_ONLY`가 **4개 문서 전부 0건** |

---

## 4. 프로젝트 회의록 260728

### 4-1. 회의록의 자기 규정 미이행

회의록 §10: "결정로그에 아직 등록되지 않은 기록채팅 결정은 **근거시각·동의범위·팀장 확인문구를 함께 남기며**". 그러나 본문 어디에도 근거시각·동의범위·확인문구·발언자가 없다. 데이터사전 0.3이 신설한 `decision_chat_datetime`·`proposal_summary`·`agreement_scope`·`final_confirmation_text`·`decision_log_id` 값도 전무하다.

### 4-2. 결정완료 5건의 근거 검증

| 항목 | 채팅 기록상 근거 | 판정 |
|---|---|---|
| Seed18 제한 파일럿 | §3 N-1로 신규 안건 분류, §5 "제안 상태(의결 전)", §7-4 "여전히 APPROVAL_PENDING … 산출범위·동결·일정(N-1~N-3)" | **낮 기록에는 근거 없음** — 명시적으로 의결 전 제안으로 분류됨 |
| P10 상대 필터 표현 | §7-4 "검산으로 닫힌 사실관계(정책 아님)"에 P10 구조 포함 → **사실 서술은 근거 있음**. 그러나 같은 절 "여전히 APPROVAL_PENDING … P10 정책" | **부분** — 사실은 맞으나 "결정완료" 분류는 과다. 핵심 사실(최저 1종목 강제탈락)은 회의록에 없음 |
| 수기검산 착수(A안 고정) | 검산 활동은 실재. 그러나 **"A안"이라는 표현이 채팅·결정로그·담당표 전문에 없음**. "팀장 진행확인" 문구·시각도 없음 | **근거 약함** |
| 단일 발표자 취합·발표 | 낮 채팅 기록에 발표 운영 언급은 §3 N-3 "발표자료 취합 **일정**" 1건뿐(운영방식 아님) ← 실측 확인 | **낮 기록에는 근거 없음** |
| 슬라이드별 담당·근거·의도·Q&A | 위와 동일 | **낮 기록에는 근거 없음** |

> 주의: 야간(22:09~00:36) 대화 원문은 확보되어 있지 않다. 위 판정은 **낮 채팅 정리본(§1~§8) 기준**이며, 야간에 별도 합의가 있었을 가능성은 배제하지 않는다. 다만 회의록이 스스로 요구한 "근거시각·동의범위·확인문구"가 기재되지 않아 문서만으로는 확인할 수 없다.

### 4-3. 담당표와의 충돌 — 발표 운영

| 문서 | 규정 |
|---|---|
| `260722_작업담당표.md` 5단계 (확정) | 78~82행: "방법론 발표자료 총괄 — **김민호(주관)**" / "구간 발표: 공시근거·PIT·검토도구=김근형 / Mandate·BM·지수 산출=권보성 / QA·검증=김민호 / 테마·기업조사·인디케이터=추주원 — **전원**" / "강사 피드백 반영·최종 제출물 정리 — **김민호(주관)**" |
| 회의록 §7 | "기존의 파트별 개별 발표 대신 **독립 QA·발표 취합 담당이 모든 슬라이드를 취합하고 단일 발표자로 진행한다**" |

**정정(2026-07-29)**: 역할 배정 v1.1 기준으로 회의록의 "독립 QA·발표 취합 담당"은 **김민호**이며, 담당표 5단계의 "방법론 발표자료 총괄 — 김민호(주관)"과 **동일인이다.** 따라서 총괄 주체는 충돌하지 않는다.

실제 충돌은 **"구간 발표: … — 전원"이 "단일 발표자"로 바뀐 부분 하나**다. 담당표 갱신 지시가 회의록에 없으므로 이 변경만 의결하고 담당표를 v1.3으로 갱신하면 된다. 최초 작성 시 이 항목을 "확정 담당표를 정면으로 뒤집는 변경"으로 과대 기술했고, 단일 발표자를 권보성으로 잘못 적었다.

### 4-4. 진행도 판정값이 자기 문서의 enum을 위반

회의록 §1 진행표: `부분완료` / `진행` / `파일럿 완료` / `운영방식 결정`
데이터사전 12.1 `stage_status` 허용값: `CONFIRMED / IN_PROGRESS / PILOT_IMPLEMENTATION / APPROVAL_PENDING / NOT_PERFORMED / HOLD`

**4개 표현 중 3개가 등록된 허용값이 아니다.** 룰북 제9조 "문서·코드 용어 일치" 요건과 어긋난다.

### 4-5. 회의록·채팅에 있으나 3개 문서에 미반영

| # | 항목 | 반영 상태 |
|---|---|---|
| 1 | P10 = 최저 1종목 강제탈락 장치(`h=(n−1)×0.10<1`, KTOS가 ATI보다 34% 큰데도 탈락) | 3개 문서 모두 "상대 하위 필터"로만 기술. 강제탈락 구조 **0건** |
| 2 | 경계구간 공집합 + `NOT_APPLICABLE` 기록방식 | 미반영 |
| 3 | KTOS = 분포 최솟값(표현 철회) | `경계 진단자료`로 존치 |
| 4 | `bm_kr.csv` = `bm_kr_krx_official.csv` 해시 동일·야후 `^KS200` 예비계열 | `Yahoo`·`야후` **4개 문서 전부 0건** |
| 5 | 미국 BM 유지 vs 재선정 안건 | 룰북 별표9 "미국 BM \| Russell 3000 PR \| 확정" 유지. 회의록에도 안건 없음 |
| 6 | `listing_board` 필드 부재 | 4개 문서 전부 0건 |
| 7 | `independence_level` / `SPEC_ASSISTED` / `QA_REFERENCE_ONLY` | 4개 문서 전부 0건. 대신 `INDEPENDENTLY_MATCHED`로 상향 표기 |
| 8 | `recalc_reason` enum(`PERFORMANCE`는 항상 금지값) | 4개 문서 전부 0건 |
| 9 | 6.6611%p 산식·유효 리밸런싱 0회·연결계수 1.0 고정 | 미반영 |
| 10 | N-2 유니버스 동결시점 · N-3 전체 일정 | 회의록·3개 문서 어디에도 없음. 채팅 §6은 "폐회 전 담당·기한과 함께 배정" 요구 |
| 11 | 라벨 체계 일원화(§4-5 경고) | 실제로 증식 — `T-`·`H-`·`S-`·`U-`·`F-U`·`D-U`·`M-` 7종 병존. 결정로그 ID 대응표는 `D-01~D-14`·`M-01~M-08`만 정의 |

### 4-6. 역방향 — 룰북에만 있고 회의록·채팅·결정로그에 근거가 없는 것

| 항목 | 위치 |
|---|---|
| "18종목 중 **15종목 편입**과 3종목 제외사유가 동일" | 룰북 A-5. 채팅은 `10/10 대조`까지만 |
| 회차별 자료마감일 `2026-03-24` / `2026-06-23`을 `CONFIRMED` | 룰북 A-3 / 데이터사전 §22. D-13 ⑧은 "정식 확정은 #30 적용시험 후" |
| "760개 SEC 원문 XLSX 기술 패키지는 완료" | 룰북 C |
| 제141조의4 회의확인 10건 | 결정로그 미결 대기열과 항목이 다름 |

---

## 5. TEAM_SHARE_MD_REVIEW_SUMMARY.md

### 5-1. 문서 성격

2026-07-28 20:56:30 생성(문서 4종 배포 22:09보다 1시간 앞섬). `stage_status: REVIEW_SUMMARY_CREATED_NO_GIT_PUSH`. **md 코퍼스 99개의 기계적 인벤토리 결과**이며, 공유 전 사람 검토 대상을 골라내는 것이 목적이다.

명시된 미수행 5건: `git_push` / `raw_git_upload` / `original_files_modified` / `theme_decision` / `final_inclusion_decision` — **기술 처리와 편입 판정을 분리한 표기이며, 팀의 상태 분리 원칙에 부합한다.**

### 5-2. 수치

| 항목 | 값 |
|---|---|
| `source_md_count` | 99 |
| `scan_rows` | 97 |
| 로컬경로·소스컨테이너 hit 있음 | 48 |
| hit 없음 | 51 |
| hit 총 추정 | 679 |

**48 + 51 = 99 ≠ `scan_rows` 97.** 2행 차이에 대한 설명이 문서에 없다. 상위 20개 파일의 hit 합계는 566으로 전체 679의 83%이며, 나머지 113건이 28개 파일에 분산돼 있다.

### 5-3. 검토 결과

| # | 항목 | 내용 |
|---|---|---|
| 1 | **코퍼스가 저장소 밖에 있음** | `copied_markdown/`, `Stage11-*`, `Stage12-*`, `원자료수집친구*` 전부 저장소에서 검색 0건. 요약본만 `check_this/`에 들어왔고 대상 99개 파일은 팀이 볼 수 없음 |
| 2 | **679건의 로컬경로 노출** | 채팅 §8-2에서 이미 지적된 `source_path=C:\Soph\projects\…` 문제와 같은 지점. 검증 담당이 대조 불가한 상태(DART-EVID-001 출처주소규칙) |
| 3 | **hit 없음 51개도 "사람 내용검토 후에만 후보"** | 검토 담당·기한·판정 기준이 요약본에 없음 |
| 4 | **파일명 mojibake·한글 혼재** | `한국 crop_code_rcpNO_4(stage3).md` 등 — `crop`은 `corp` 오기로 보임(추정). KR9 검증에서 확인된 파일명 mojibake 30건과 같은 계열 문제 |
| 5 | **계수 불일치** | 99 vs 97 (§5-2) |
| 6 | **상위 파일이 전부 Stage11~12 산출물** | `Stage12-0 Human Review Queue InputContract.md`(104건)가 최다. 미국 원문 사람판정 대기열의 입력계약 문서 — 룰북 C "760개 SEC 원문… human review 미수행"과 직결 |

---

## 6. 회의 사항 종합

같은 사안이 여러 문서에 걸친 것은 하나로 묶었다. 우선순위는 후속 결정에 대한 의존도 기준이다.

### A. 결정로그와의 충돌 — 가장 먼저 닫아야 함

| # | 사안 | 관련 문서 | 닫아야 할 질문 |
|---|---|---|---|
| A1 | **BM 지역비중** — D-10 ④ `확정·전원 동의`(지수 지역비중 동일 연동), 고정 50:50은 `기각`. 3개 문서가 두 후보 병기·APPROVAL_PENDING으로 되돌림 | 룰북 제124·125조 / 수식부록 §8.8·§15.6 / 데이터사전 `benchmark_official_status` | 확정 복원인가, 재논의 개시인가. 재논의라면 §0.1 확정사항 재논의 금지와의 관계 |
| A2 | **ADTV90 하한 분포 모집단** — D-07 `전체 유니버스` → v1.5 제67조 `테마 통과 후보` `[확정]` | 룰북 제66·67조 / 수식부록 §11.1 | 모집단 변경 승인 여부. 변경 시 기존 P5~P75 전량 재산출 |
| A3 | **확정 5건의 지위 격하** — D-12 ①(시장범위)·D-11 #3(제외증권)·D-10 ③(재분배)·D-12 ②(근거승계)·D-11 #30(관측일) | 룰북 제13·15·16·21·109·114조 | 결정 취소인가 표기 오류인가 |
| A4 | **미결 4건의 확정 승격** — 환율 제129~132조가 D-08 ③·D-09 ①의 `[세부 미결 유지]`를 `[확정]`으로 | 룰북 제129~132조 | 승격 승인 여부와 결정ID 부여 |
| A5 | **5개 인디케이터 `[확정]` 승격** — 미결 안건 A(A-4·A-5 강사 확인)가 v1.5에서 소멸 | 룰북 제53조 | 안건 A 종결 여부 |
| A6 | **삭제된 확정·기각문 복원** — D-08 ④(테마 ETF 비교군)·D-08 ⑤(근사 TR 기각)·D-12 ⑤(TRADING_HALT 우선)·D-12 ⑥(S 4단 분류, 하한 6조건, 자동제외 금지)·D-12 ⑧(타이브레이커 리터럴 동결)·D-13 ④⑦⑧(부록 A/B/C) | 룰북 §12.2·12.4 / 데이터사전 §4.2·§6·§9.2·부록 | 삭제 승인인가 누락인가 |
| A7 | **Mandate.md의 지위** — v1.5에 `Mandate` 0회, 제9조 우선순위 체인에 부재. Mandate 10행은 "본 문서와 룰북이 충돌할 경우 본 문서가 우선한다" | 룰북 제9조 | 상위규정 폐지인가 누락인가 |
| A8 | **삭제된 필드 8개와 별칭 매핑** — 전부 D-12·D-13 확정 산물, 대체 매핑 없음 | 데이터사전 §19 | 삭제 승인 + 무손실 매핑표 작성 |
| A9 | **boolean 저장 표준 1/0 → TRUE/FALSE** — v0.2가 "재확정 대기"로 명시한 항목 | 데이터사전 §0.1 | 전환 승인 여부(엔진 입력계약 파싱 영향) |
| A10 | **`role_grade` 5값 개명 / `theme_tiebreaker_stage` 되돌림** — D-11 #8·D-12 ⑧ 관련 | 데이터사전 §8.1·§9.2 | 개명 승인 + 별칭 매핑, 고정 서열 리터럴 복원 |

### B. 결정로그 근거 없는 신설 — 등록 필요

| # | 사안 | 관련 문서 |
|---|---|---|
| B1 | **Seed18 제한 파일럿의 결정ID** — 결정로그에 `Seed18` 0건. 유니버스 범위를 D-12 ① 확정범위에서 18종목으로 한정하는 중대 결정 | 룰북 A-1 / 데이터사전 §16 / 수식부록 §11.1 / 회의록 §2 |
| B2 | **기록채팅 승격 규칙** — "제안 뒤 명시적 동의·팀장 확인·후속 구현 수용이 이어지면 팀 결정으로 반영". `decision_source_type=RECORDED_CHAT`, `CONFIRMED_BY_RECORDED_CHAT` 상태값 | 룰북 제8조 / 데이터사전 §0.3 |
| B3 | **상태코드 체계 단일화** — 룰북 공식 7종(`CONFIRMED`~`TRANSFERRED`)에 없는 `PILOT_CONFIRMED`·`PILOT_CONFIRMED_WITHIN_SEED18`·`CONFIRMED_BY_RECORDED_CHAT`이 문서마다 다르게 사용 | 3개 문서 전부 |
| B4 | **`evidence_grade` 4단 코드표(A/B/C/D)** — 문서 스스로 "팀 승인 전"인데 매핑표가 이미 `gate_status` 자동판정 경로를 규정하고 다단계 선발 2차가 정렬순서에 의존 | 데이터사전 §17.1·§17.2 |
| B5 | **`adtv_missing_policy` 3안 채택 + `minimum_valid_days` 수치** — 필수 Y enum에 `UNDECIDED`가 값으로 존재. `official_adtv90` 생성이 이 정책에 차단됨 | 데이터사전 §5 / 룰북 제61조 / 수식부록 §3.6 |
| B6 | **P10을 필드 리터럴로 동결** — `threshold_method=SEED18_MARKET_CYCLE_P10`, `threshold_percentile=0.10`. 채팅은 P10 정책을 APPROVAL_PENDING으로 명시 | 데이터사전 §18 / 수식부록 §11.1 |
| B7 | **PR 산식(연결계수 vs 제수)** — `CHAIN_REBASE_PILOT` 근거 0건. "1.1e-15는 자기재현이지 동치증명 아님"으로 양측 합의 → 의결 필요성 강화 | 수식부록 §12.2 / 룰북 B-2 |
| B8 | **MACD 12·26·9와 EMA 초기화** — 결정로그 근거 없이 룰북이 `[확정]` 표기 | 룰북 제83조 / 수식부록 §15.3 |
| B9 | **회차별 `data_cutoff_date` `CONFIRMED` 표기** — D-13 ⑧은 "적용시험 후 확정" | 룰북 A-3 / 데이터사전 §22 |
| B10 | **원자료 승격체인** `RAW_SOURCE→QA_PASSED→PEER_CHECKED→TEAM_APPROVED→ENGINE_INPUT` | 룰북 A-4 / 데이터사전 §17 |

### C. 문서 간·문서 내 불일치 — 엔진 구현 전 필수

| # | 사안 | 영향 |
|---|---|---|
| C1 | **`theme_eligibility_status` 4중 규정**(별칭 vs 독립 필수 필드) | 판정원장 스키마·매핑표가 이 필드에 의존 → 엔진 구현이 갈림 |
| C2 | **enum 다중정의 11건** (§2-3) | 입력계약 자동검사가 성립하지 않음 |
| C3 | **허용값 미완결 필수 Y 필드 7종** ("등"으로 끝나거나 목록 부재) | 같음 |
| C4 | **승인 상태 3계열 값 체계 불일치 + `PENDING` 미정의** | 상태 판정 자동화 불가 |
| C5 | **필드명 불일치 20여 건**(수식부록 ↔ 데이터사전 ↔ 룰북 별표) + `level_gap` 미등록 + ADTV90 출력 필드명 3종 | 룰북 제9조 "충돌 발견 시 임의 적용하지 않고 실행 보류" 발동 조건 |
| C6 | **`source_url` / `source_url_or_path` / `source_path` 3필드** — D-13 ④는 단일화 확정 | 정정계보·테마근거·PIT가 각기 다른 필드 사용 |
| C7 | **당좌비율 차감식 계정범위** — 재고만 vs 3계정 | 값 자체가 달라짐 |
| C8 | **효력발생일 "정기변경 적용주" 조건 탈락** — D-12 ⑩보다 좁은 식 | 규칙 축소가 룰버전 기록 없이 발생 |
| C9 | **라벨 체계 7종 병존**(`T-`·`H-`·`S-`·`U-`·`F-U`·`D-U`·`M-`) + `F-U04`의 `D-08` 축약이 대응표상 다른 결정을 지시 | 결정 추적 불가 |
| C10 | **데이터사전 장번호 중복 7건 / 수식부록 장번호 중복 4건** | 조항 인용 자체가 불가 |
| C11 | **3개 문서 공통 `rule_version` 값 미선언** | 별표14 "동일 rule_version으로 연결" 미충족 |
| C12 | **수식부록 식 1~62 본체 부재** | 연산명세 문서만으로 독립 재현·검산 불가(룰북 제136조) |
| C13 | **진행도 판정값이 `stage_status` enum 위반** | 회의록 §1 |

### D. 사실관계 반영 누락 — 검산으로 이미 닫힌 것

| # | 사안 |
|---|---|
| D1 | P10 = 최저 1종목 강제탈락 장치(수학+실측). 3개 문서 전부 "상대 하위 필터"로만 기술 |
| D2 | 경계구간 [P5,P10] 공집합 + `NOT_APPLICABLE` 기록방식 |
| D3 | KTOS = 분포 최솟값(철회된 "경계사례" 표현이 룰북 A-2·수식부록 §11.2·§14.3에 존치) |
| D4 | `bm_kr` 파일명·내용 불일치 실측(해시 동일·야후 예비계열). "2.184e-16 = 같은 파일 사용" 표현 정정 |
| D5 | `independence_level`·`SPEC_ASSISTED`·`QA_REFERENCE_ONLY` 도입, `INDEPENDENTLY_MATCHED` 표기 하향 |
| D6 | 6.6611%p 산식·유효 리밸런싱 0회·연결계수 1.0 고정 |
| D7 | `SAME_DAY_ECOS` 명세(기준 시장·매매기준율/종가환율) |
| D8 | `recalc_reason` enum과 `PERFORMANCE` 금지값 |
| D9 | 010120 `split_ratio=5.0`의 근거상태 — 원공시 3.33:1 vs 정정본 5:1, 접수번호 확보 중, 현 근거 언론 3건 |

### E. 역할·운영

| # | 사안 |
|---|---|
| E1 | **발표 운영방식 변경 의결과 담당표 갱신** — 총괄 주체(김민호)는 담당표와 동일하므로 정합. 충돌은 담당표 "구간 발표 — 전원"이 회의록 "단일 발표자"로 바뀐 부분뿐이며, 이 변경만 의결 후 담당표 v1.3 갱신 필요 |
| E2 | **수기검산 "A안 고정"의 정의** — 채팅·결정로그·담당표 전문에 "A안" 없음 |
| E3 | **N-2 유니버스 동결시점 · N-3 전체 일정 배정**(담당·기한) — 회의록에 항목 자체가 없음 |
| E4 | **미결 항목의 담당·기한 필드 부재** — 룰북 4개 미결 목록·데이터사전 `APPROVAL_PENDING` 12개소 전부 담당·기한 없음. 결정로그 운영원칙은 "필요한 자료와 담당자를 남긴다" |
| E5 | **개인 이름 필드 신설 vs 익명화 원칙** — `role_owner`·`presentation_owner`·`slide_owner`·`qna_owner` vs D-13 ④ "reviewer 실명 대신 익명 슬롯·역할코드" |
| E6 | **공시추적원장 컬럼 확장 범위** — 채팅 합의는 축소(2건+`rcept_no` 1컬럼), 회의록·데이터사전은 6개 대응필드 전체로 확대 |
| E7 | **회의록 자기 규정 이행** — 근거시각·동의범위·팀장 확인문구 기재 |

### F. 실무 조치

| # | 사안 |
|---|---|
| F1 | `listing_board` 필드 신설 — 없으면 D-13 ③(KRX 300 재확인) 발동조건 판단 자체가 불가 |
| F2 | **미국 BM 유지 vs 재선정** — 룰북 별표9는 여전히 "확정". 결정로그 D-08의 검토 대안은 절차·TR 방식이며 미국 지수 후보 비교 기록 없음 |
| F3 | **중간 버전 부재** — 룰북 v1.3·v1.4, 데이터사전 v0.3~v0.5, 수식부록 원본이 저장소에 없음. 변경관리 기록 원칙이 끊긴 상태 |
| F4 | **버전 문자열 중복** — 22:09 배포본과 23:58 재배포본이 같은 `v0.6-review`·`v1.4-review`. 22:09의 렌더링·육안검사 QA는 대체된 파일 대상 |
| F5 | **md 코퍼스 99개의 인계 범위** — 저장소 밖에 있고 로컬경로 hit 679건. hit 없음 51개의 사람검토 담당·기한 미정. 계수 불일치(99 vs 97) |

---

## 7. 검증 수준

### 실측으로 직접 확인한 항목

| # | 확인 내용 | 결과 |
|---|---|---|
| 1 | 룰북 v1.5 내 `Mandate` 출현 | 0회 (v0.4는 3회) |
| 2 | 결정로그 내 `Seed18` 출현 | 0회 |
| 3 | 룰북 v1.5 내 `강사` 출현 | 0회 (v0.4는 안건 A로 존재) |
| 4 | v0.4 199행 하한 모집단 = "시장별 전체 유니버스" ↔ v1.5 303행 "테마 통과 후보" | 확인 |
| 5 | v0.4 170·342행 인디케이터 5개 강사 확인 대기 ↔ v1.5 246행 `[확정]` | 확인 |
| 6 | 룰북 v1.5 제129~132조 전부 `[확정]` 태그 | 확인 |
| 7 | 결정로그 D-20260721-10 ④ 원문 + 기각 사유 "BM 고정안: 예외 효과가 알파로 오독됨" ↔ 룰북 450행 두 후보 병기 | 확인 |
| 8 | 결정로그 D-20260720-08 검토 대안 = 절차·근사 TR 2건, 미국 지수 후보 비교 기록 없음 | 확인 |
| 9 | v0.4 265·277행 근사 TR 기각·ARKK 조항 ↔ v1.5 해당 문자열 0회 | 확인 |
| 10 | 데이터사전 v0.6 내 `listing_board` | 0회 |
| 11 | 데이터사전 v0.6 775행 `split_ratio` "010120 확인값 5.0" | 확인 |
| 12 | 데이터사전 v0.6 111행 "current_ratio를 quick_ratio의 별칭으로 사용하지 않는다" | 확인 |
| 13 | `260722_작업담당표.md` 78~82행 5단계 발표 배정 | 확인 |
| 14 | 낮 채팅 정리본 내 `발표` 출현 = 1회(N-3 일정 항목) | 확인 |
| 15 | 저장소 내 수식부록·md 코퍼스 존재 여부 (`find`) | 0건 |

### 확인하지 못한 것

- **야간(22:09~00:36) 대화 원문** — 회의록 결정완료 항목의 야간 합의 여부를 문서만으로는 판별할 수 없다. §4-2의 "근거 없음" 판정은 낮 기록 기준이다.
- **수식 본체** — 수식부록의 식 1~62가 이미지 조판이라 텍스트로 검증하지 못했다. §3-2·§3-3의 판정은 본문 서술과 개정장 코드블록 기준이다.
- **엔진 코드(`06_코드/engine`)와의 대조** — 필드명 불일치가 실제 코드에서 어떻게 처리되는지는 확인하지 않았다.
- **개별 필드 470개 전수** — 신설 156·삭제 8은 표 1열 기준 집계이며, 설명문 내부 언급까지 포함한 전수는 아니다.

---

*본 문서는 배포본과 저장소 원본의 대조 결과이며 팀의 판단을 대신하지 않는다. "회의 사항"은 의결 대상 후보이고, 어느 항목도 결정이 아니다. 최종 문안은 결정로그 등록을 거쳐 승인 규칙으로 승격한다.*
